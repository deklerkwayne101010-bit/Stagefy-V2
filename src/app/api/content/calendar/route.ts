[{'next/server': 'import { createClient'}, {")) {\n    return null;\n  }\n\n  const token = authHeader.replace('Bearer ', '": 'const supabase = createClient(supabaseUrl', 'data': {'GET(request': "NextRequest) {\n  try {\n    const user = await getUserFromAuthHeader(request);\n    if (!user?.id) {\n      return NextResponse.json({ error: 'Authentication required'"}, 'status': 401}, {'ascending': True}, {'calendar': ', error);\n      return NextResponse.json({ error:', 'status': 500}, {'entries': 'data || []'}, {'error': ', error);\n    return NextResponse.json({ error: error.message }, { status: 500 });\n  }\n}\n\n// POST /api/content/calendar - Create single calendar entry\nexport async function POST(request: NextRequest) {\n  try {\n    const user = await getUserFromAuthHeader(request);\n    if (!user?.id) {\n      return NextResponse.json({ error:', 'status': 401}, {'error': 'Missing required fields: title, scheduled_date'}, {'status': 400}, {'user_id': 'user.id', 'hashtags': 'hashtags || []', 'template_type': "template_type || 'professional'", 'is_recurring': 'is_recurring || false', 'recurrence_pattern': 'recurrence_pattern || {'}, {'entry': ', error);\n      return NextResponse.json({ error:', 'status': 500}, {'entry': 'data'}, {'error': ", error);\n    return NextResponse.json({ error: error.message }, { status: 500 });\n  }\n}\n\n// PATCH /api/content/calendar/[id] - Update entry\nexport async function PATCH(\n  request: NextRequest,\n  context: { params: Promise<{ id: string }> }\n) {\n  try {\n    const user = await getUserFromAuthHeader(request);\n    if (!user?.id) {\n      return NextResponse.json({ error: 'Authentication required"}, {'status': 401}, {"title', 'content_type', 'platform', 'caption', 'hashtags',\n      'template_type', 'template_prompt', 'scheduled_date',\n      'status', 'is_recurring', 'recurrence_pattern": 'const updateData: any = {'}, {'entry': ", error);\n      if (error.code === 'PGRST116') {\n        return NextResponse.json({ error: 'Entry not found"}, {'status': 404}, {'error': 'Failed to update entry'}, {'status': 500}, {'entry': 'data'}, {'error': ", error);\n    return NextResponse.json({ error: error.message }, { status: 500 });\n  }\n}\n\n// DELETE /api/content/calendar/[id] - Delete entry\nexport async function DELETE(\n  request: NextRequest,\n  context: { params: Promise<{ id: string }> }\n) {\n  try {\n    const user = await getUserFromAuthHeader(request);\n    if (!user?.id) {\n      return NextResponse.json({ error: 'Authentication required"}, {'status': 401}, {'entry': ", error);\n      if (error.code === 'PGRST116') {\n        return NextResponse.json({ error: 'Entry not found"}, {'status': 404}, {'error': 'Failed to delete entry'}, {'status': 500}, {'success': True}, {'error': ', error);\n    return NextResponse.json({ error: error.message }, { status: 500 });\n  }\n}'}]